name: Build and Release OBAN APK

on:
  push:
    branches: [ main ]

jobs:
  build:
    name: Build Release APK
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Scaffold Android Project
        run: |
          cp pubspec.yaml pubspec.yaml.bak
          flutter create --org com.obanstudio --project-name oban_downloader --platforms android .
          cp pubspec.yaml.bak pubspec.yaml
          if [ -d "android_custom" ]; then
            cp -rf android_custom/* android/
          fi

      - name: Get Dependencies
        run: flutter pub get

      - name: Compile Release APK
        run: flutter build apk --release

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: OBAN-Downloader-APK
          path: build/app/outputs/flutter-apk/app-release.apk

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v2.0.${{ github.run_number }}
          name: "OBAN Downloader v2.0 (Build #${{ github.run_number }})"
          body: |
            ### Обновление OBAN Downloader v2.0
            - Добавлена очередь загрузок (Multi-Download Queue)
            - Автоматическое сохранение в Галерею (Movies/Music + MediaScanner)
            - Встроенный видеоплеер и мини-аудиоплеер для фонового воспроизведения
            - Исправлена работа кнопки «Поделиться» из YouTube
            - Полностью удалён ИИ для максимальной скорости и безопасности
          files: build/app/outputs/flutter-apk/app-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

name: goalflow
description: "GoalFlow — трекер целей и накоплений"
publish_to: 'none'
version: 7.1.0+3

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  uuid: ^4.3.3
  http: ^1.2.0
  provider: ^6.1.2
  flutter_local_notifications: ^17.1.2
  path_provider: ^2.1.2
  share_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
